# Node modules (because uploading half the internet is a bad idea)
node_modules/

# Environment variables (because secrets don't belong on GitHub)
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS junk
.DS_Store
Thumbs.db
